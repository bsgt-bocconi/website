---
import Mark from './Mark.astro';

const nav = [
  { href: '/', label: 'About' },
  { href: '/team', label: 'Team' },
  { href: '/articles', label: 'Articles' },
  { href: '/events', label: 'Events' },
];

const path = Astro.url.pathname;
const isCurrent = (href: string) => path === href || path.startsWith(`${href}/`);
---

<header class="site-header on-dark">
  <div class="wrap bar">
    <a href="/" class="brand" aria-label="BSGT — home">
      <span class="mark"><Mark size={30} weight="bold" /></span>
      <span class="wordmark">BSGT</span>
    </a>
    <nav aria-label="Primary">
      <ul>
        {
          nav.map((item) => (
            <li>
              <a href={item.href} aria-current={isCurrent(item.href) ? 'page' : undefined}>
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>

<style>
  .site-header {
    background: var(--color-navy-deep);
    color: var(--color-paper);
  }

  .bar {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem 2rem;
    padding-block: 1.25rem;
  }

  .brand {
    display: inline-flex;
    align-items: baseline;
    gap: 0.375rem;
    text-decoration: none;
    color: var(--color-gold);
  }

  .mark {
    display: inline-flex;
    /*
     * Optical alignment: the icon's bounding box is taller than "BSGT"'s
     * cap-height (Newsreader's caps run ~0.7em of its 22px size ≈ 15.4px,
     * against a 30px icon), so centering on the box would read as
     * icon-heavy and low. align-items: baseline puts the icon's bottom
     * edge on the text baseline; this shifts it down so its optical
     * centre lands on the cap-height band instead. Approximate — revisit
     * once this can be checked visually.
     */
    transform: translateY(7px);
  }

  .wordmark {
    font-family: var(--font-serif);
    font-weight: 600;
    font-size: 1.375rem;
    line-height: 1;
    letter-spacing: 0.02em;
    color: var(--color-paper);
  }

  nav ul {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1.75rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  nav a {
    display: inline-block;
    padding-block: 0.25rem;
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    text-decoration: none;
    color: var(--color-paper);
    border-bottom: 1px solid transparent;
  }

  nav a:hover {
    color: var(--color-gold);
  }

  nav a[aria-current='page'] {
    color: var(--color-gold);
    border-bottom-color: var(--color-gold);
  }
</style>
