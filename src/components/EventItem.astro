---
import type { CollectionEntry } from 'astro:content';
import { capitalize, formatDate } from '../lib/format';

interface Props {
  entry: CollectionEntry<'events'>;
}

const { entry } = Astro.props;
const { title, date, startTime, location, type, description, registrationUrl } = entry.data;
---

<div class="event">
  <p class="type">{capitalize(type)}</p>
  <h3>{title}</h3>
  <p class="meta">
    <time datetime={date.toISOString()}>
      {formatDate(date)}
      {startTime && `, ${startTime}`}
    </time>
    <span>{location}</span>
  </p>
  <p class="description">{description}</p>
  {
    registrationUrl && (
      <p class="register">
        <a href={registrationUrl}>Register</a>
      </p>
    )
  }
</div>

<style>
  .type {
    font-family: var(--font-sans);
    font-size: var(--text-xs);
    /* Not slate — fails AA on the events page's navy-deep ground. */
    color: var(--color-paper);
    opacity: 0.75;
  }

  h3 {
    margin-top: 0.375rem;
    font-family: var(--font-serif);
    font-size: var(--text-xl);
  }

  .meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem 0.875rem;
    margin-top: 0.5rem;
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    color: var(--color-paper);
    opacity: 0.75;
  }

  .description {
    margin-top: 0.75rem;
    max-width: var(--measure);
    font-family: var(--font-sans);
    font-size: var(--text-base);
    line-height: var(--leading-normal);
    /* Not ink — near-invisible on navy-deep. */
    color: var(--color-paper);
  }

  .register {
    margin-top: 0.75rem;
    font-family: var(--font-sans);
    font-size: var(--text-sm);
  }

  .register a {
    /* Not navy-deep — identical to the page background, invisible. */
    color: var(--color-paper);
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 0.15em;
  }

  .register a:hover {
    color: var(--color-gold);
  }
</style>
