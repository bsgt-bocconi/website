// Single source of truth for BSGT's contact email — same pattern as
// src/data/divisions.ts. Referenced from every page that needs it
// (currently just the privacy policy) instead of being hardcoded per
// page, so there's exactly one place to update if it ever changes.
export const CONTACT_EMAIL = 'as.bsgt@unibocconi.it';
